1차 프로젝트
	HTML, JAVA, DB, 3가지 진영을 모두 볼 수 있어야 한다.


	비동기: 뭘 눌러야(이벤트 발생) 보이는 데이터들
	동기: 페이지에 도착하면 바로 보여야 하는 데이터들

	작성
		INSERT
			인서트할땐 VO 가 기본, 행의 정보(ex. id)가 필요할 때에는 DTO
			input 태그의 name은 DTO 보고 쓰기

			VO 기본
			※ INSERT된 행의 정보(id)가 필요할 때에는 DTO!


	조회
		SELECT
			웬만하면 조건이 id임..
			
			Optional객체로 랩핑한다.
			orElseThrow(Object::new);
			ExceptionHandler

	목록
		SELECT
			페이징 처리(페이징, 더보기, 무한 스크롤) - 더보기,무한 스크롤은 비동기임
			- Criteria
				1. 페이징
					page
					
					rowCount
					pageCount
					startPage
					endPage
					offset

				2. 더보기, 무한 스크롤(JS)
					rowCount + 1

		
			검색
				type, keyword
				※ 통합 검색: keyword

			필터
				체크박스(누적)
				1. 일부만 포함되도 가져오기
					배열로 받자!
					동적쿼리
					<foreach item="변수명" collections="반복자" seperator=",">
					<if test="">
					<choose><when test=""><otherwise>
					<trim prefix="" suffix="" prefixOverrides="" suffixOverrides="">

				2. 전부 다 포함해야 가져오기
					group by
					전달받은 태그인 정보 모두 가져온다.
					해당 정보 중 동일한 id는 묶어서 개수를 집계한다.

					id	tag	fk_id
					1	A	1
					2	B	1
					3	A	2
					4	A	3

					fk_id	개수	조회 결과
					1	2	O
					2	1	X
					3	1	X
				

	수정
		UPDATE
			1:N 관계에서 N를 수정하는 경우
			한 번에 다 받아오자
			처리는 분리해서!!

			수정 X
			삭제 및 추가 O

			예1) 태그 목록
			처리: 삭제 목록 + 추가 목록

			예2) 파일 목록
			처리: 삭제 목록 + 추가 목록	

			파일인 경우 반드시 실제 경로도 삭제해줘야 한다.
			즉, 파일 삭제 전 조회(파일 경로 및 파일 이름)가 필수이다.		

	삭제
		UPDATE(Soft)
		DELETE(Hard)
			반드시 자식 테이블부터 삭제한다.


AOP(Aspect Oriented Programming) - 내가 작성하지않은 코드가 실행된다면 AOP영역에서 실행된것. 이라는걸 알자
	관점이란 개발에 있어서 관심사(Concern)을 의미한다.
	코드의 중복을 줄일 수 있으며, 핵심 로직과 주변 로직을 분리하여 관리할 수 있다.

	- 파라미터가 잘 전달 되었는가?
	- 이 로직에서 발생할 수 있는 예외가 무엇인가?

	핵심 로직은 아니지만 반복적으로 개발에 필요한 관심사들을 주변 로직이라고 한다.
	따라서 AOP는 이러한 주변 로직을 횡단 관심사로 분리하여 작성하고
	종단 관심사인 핵심 비지니스 로직만을 작성하도록 한다.

	예) 나눗셈 프로그램 개발 시 두 개의 숫자를 나누는 것(핵심)
	     0으로 나누는지 검사하는 것(주변)

	즉, 반복적으로 나타나는 횡단 관심사를 모듈로 분리한 후 적절한 시점에
	로직을 주입하는 것이 AOP이다. 스프링에서는 별도의 복잡한 설정 없이 간편하게
	AOP의 기능들을 구현할 수 있기 때문에 중요한 특징 중 하나이다.


AOP 용어
	Aspect: 무엇을 어디에서 할 것인가? 여러 곳에서 공통적으로 사용하는 주변로직 모듈의 묶음
	Adivce: 어떤 동작을 언제 할 것인가? 실제 실행될 코드를 작성해 놓은 모듈
	Joinpoint: 적용 가능한 모든 시점. Advice가 적용될 수 있는 시점
	Pointcut: 실제 작업을 수행할 타겟을 정의하는 필터

	
	예시)
	연예인: Target
	매니저: Proxy

	Aspect: 매니저가 해야 할 일을 정의한 문서
	Joinpoint: 연예인에게 진행 가능한 모든 스케줄(영화, 광고, 예능)
	Pointcut: 광고만 진행하겠다고 연예인이 말함
	Advice: 매니저가 계약서 작성

Joinpoint
	- Around (전 구역)
	- Before (메소드 시작 직후)
	- After (메소드 종료 직전)
	- AfterReturning (메소드 리턴 후)
	- AfterThrowing (메소드 예외 발생 후)

AOP 설계 순서
	1. 구현할 횡단 관심사를 의미할 수 있는 어노테이션 만들기
	2. 어노테이션을 AOP로 등록하기
	3. 종단 관심사에 등록된 어노테이션 사용하기























